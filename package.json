{
  "name": "k8s-pod2pod-log",
  "version": "1.0.0",
  "description": "a sidecar image to log all pod's HTTP traffic for both incoming and outgoing requests.",
  "main": "cap.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sadegh razaghi",
  "license": "ISC",
  "dependencies": {
    "cap": "^0.2.1",
    "elasticsearch": "^16.7.1",
    "fluent-logger": "^3.2.0",
    "logstash-client": "^1.1.1"
  }
}
